Django==6.0.7
celery[redis]==5.6.3
coverage==7.15.2
django-cors-headers==4.9.0
django-environ==0.14.0
django-extensions==4.1
django-filter==25.2
django-import-export==4.4.1
django-parler==2.4
django-schema-viewer==0.5.3
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
drf-nested-routers==0.95.0
drf-spectacular==0.30.0
PyYAML==6.0.3
pytest==9.1.1
pytest-django==4.12.0
requests==2.34.2
ruff==0.16.0
reportlab
sentry-sdk[django,celery]==2.66.1
nh3==0.3.6
user-agents==2.2.0
geoip2==4.8.1
# PostgreSQL driver. Optional at runtime — only loaded when
# ``DATABASE_URL`` resolves to a postgres scheme. Keep installed so the
# prod EC2 (which is migrating off SQLite) and any RDS-bound future
# host find the driver without a separate pip pass on cut-over.
psycopg[binary]==3.3.4
