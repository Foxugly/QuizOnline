import {LanguageSwitcherComponent} from './language-switcher';

describe('LanguageSwitcherComponent', () => {
  it('exports the component', () => {
    expect(LanguageSwitcherComponent).toBeTruthy();
  });
});
